#!/bin/bash

. k8s-read-config "$@"

echo "Building $DOCKERTAG from $BASEDIR/$DOCKERFILE"
echo "Build args: $BUILD_ARGS"
docker build --rm=false -t $DOCKERTAG -f ${BASEDIR}/$DOCKERFILE ${BASEDIR} --build-arg $BUILD_ARGS
if [ $? -ne 0 ]
then
  echo "Docker build failed! Aborting"
  exit 1
fi
